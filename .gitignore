# Large raw data files are tracked via Git LFS (see .gitattributes).
# IMD rainfall and Copernicus DEM tiles are large — pull with `git lfs pull`.
data/interim/

# Superseded synthetic stand-in from the first day. If these are still tracked:
#   git rm --cached data/processed/MOCK_ner_landslide_v0.csv gis/MOCK_*.geojson
MOCK_*

__pycache__/
*.pyc
